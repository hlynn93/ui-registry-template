---
title: Example Form
description: A contact form with Zod validation.
---

<ComponentBlock
  name="Example Form"
  description="A contact form with Zod validation."
  type="block"
  path="aui/blocks/example-form/example-form.tsx"
/>

## Installation

```bash
npx shadcn@latest add https://acme.com/r/example-form.json
```

This will install the component along with its dependencies (`zod`) and registry dependencies (`button`, `input`, `label`, `textarea`, `card`).

## Usage

```tsx
import { ExampleForm } from "@/components/example-form";

export default function Page() {
  return <ExampleForm />;
}
```

## Props

<AutoTypeTable
  path="./registry/aui/blocks/example-form/type.ts"
  name="ExampleFormProps"
/>
